// This service has been deprecated as the app is now a static library.
// No active AI generation code remains.

import { Activity } from '../types';

export const getExpertChat = (): any => {
  return null;
};

export const generateCustomActivity = async (prompt: string): Promise<Activity | null> => {
  return null;
};